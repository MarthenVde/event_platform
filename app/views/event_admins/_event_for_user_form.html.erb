<%= form_with(model: event, url:  create_apply_to_host_event_path , method: "post", local: true) do |form| %>
<% if user.errors.any? %>
<div class="alert alert-danger" role="alert">
  <h4><%= pluralize(event.errors.count, "error") %> prohibited event from being saved:</h4>
  <hr>
  <p class="mb-0">
  <ul>
    <% event.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
  </p>
</div>
<% end %>
<div class="form-row">
  <div class="form-group col-md-6">
    <%= form.label :event_name %>
    <%= form.text_field :event_name, class: 'form-control', placeholder: :event_name, required: true%>
  </div>
</div>
<div class="form-row">
  <div class="form-group col-md-6">
    <%= form.label :event_description %>
    <%= form.text_field :event_description, class: 'form-control', placeholder: :event_description, required: true%>
  </div>
</div>

<%= form.submit :Apply, class: "btn btn-primary" %>
<% end %>
